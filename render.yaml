services:
  - type: web
    name: myai
    runtime: python
    build:
      pythonVersion: 3.11.11
      commands:
        - poetry install
    start: poetry run uvicorn main:app --host 0.0.0.0 --port $PORT